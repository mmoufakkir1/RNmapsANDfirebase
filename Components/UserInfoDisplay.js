import React from 'react';
import { StyleSheet, Text, View, Image, Modal, Animated } from 'react-native';
import { isAbsolute } from 'path';

export default class UserInfoDisplay extends React.Component {
    render() {
        return (
            <View style={{ height: 120, zIndex: 100, backgroundColor: red, position: isAbsolute, bottom: (height / 4.5) }}>
                <Text> hello </Text>
            </View>
        )
    }
}